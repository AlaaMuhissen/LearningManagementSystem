// Central API base URL — change this one value to switch between local and production
const API_URL = import.meta.env.VITE_API_URL || 'https://learningmanagementsystem.onrender.com';
export default API_URL;