import { Router } from "express";

const router = Router();


router.get("/getAllExercises" ,);
router.get("/getExercise/:id", );
router.post("/addNewExercise",);
router.put("/updateExercise/:id",)
router.delete("/deleteExercise/:id" , );