import { Router } from "express";

const router = Router();

router.get("/getAllExams" ,);
router.get("/getExam/:id", );
router.post("/addNewExam",);
router.put("/updateExamDetails/:id",)
router.delete("/deleteExam/:id" , );